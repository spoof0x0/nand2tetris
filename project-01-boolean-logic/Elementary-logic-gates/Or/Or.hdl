/**
 * Or gate:
 * if (a or b) out = 1, else out = 0 
 * A OR B = NOT( NOT A AND NOT B ) De Morgan's Law
 */

 
CHIP Or {
    IN a, b;
    OUT out;

    PARTS:
    Not(in=a , out=notA );
    Not(in=b , out=notB );
    Nand(a=notA , b=notB , out=out ); 
}